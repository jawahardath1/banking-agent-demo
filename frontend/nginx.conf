server {
 listen 80;
 server_name localhost;
 root /usr/share/nginx/html;
 location /api/ { proxy_pass http://backend:8000; proxy_read_timeout 180s; }
 location / { try_files $uri $uri/ /index.html; }
}
